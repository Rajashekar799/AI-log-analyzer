services:
  - type: web
    name: ai-log-analyzer
    env: python
    buildCommand: pip install -r ai-log-analyzer/requirements.txt
    startCommand: uvicorn ai-log-analyzer.app:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
